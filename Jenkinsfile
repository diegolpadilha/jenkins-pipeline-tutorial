pipeline {
    agent {
        label; 'maven'
    }

    stages {
        stage('Compile') {
            steps {
                echo 'Stage Compile'
            }
        }

         stage('Compile') {
            steps {
                echo 'Stage Compile'
            }
        }
    }
}